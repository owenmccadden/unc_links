import React from 'react'

const Course = (props) => {
    return (
        <div>
            {props.links.map(link => {
                
            })}
        </div>
    )
}

export default Course
